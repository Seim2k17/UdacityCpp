#include "processor.h"

// TODO: Return the aggregate CPU utilization
float Processor::Utilization() {
  float count(0.5);
  /*
  for (int i = 1; i < 10; i++) {
    count += (1/i);
  }
  */
  return count;
}